# The local JDK is newer than the ASM bundled by ArchUnit. These rules inspect direct constant-pool
# dependencies only, so resolving missing transitive JDK dependencies is unnecessary and noisy.
resolveMissingDependenciesFromClassPath=false
